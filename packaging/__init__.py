"""Loki: deterministic guardrails for AI coding agents.

The engine is the single file ``loki.py``; ``loki init`` copies it into each
repository's ``.loki/`` directory so hooks run a pinned, reviewed engine.
"""
